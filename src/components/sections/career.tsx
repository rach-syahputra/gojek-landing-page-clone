import Button from '../elements/button'
import SectionHeader from '../elements/section-header'

export default function Career() {
  return (
    <section className='flex flex-col items-center justify-center gap-4 px-4 pb-32 pt-20'>
      <SectionHeader>We’re Gojek, the drivers of change</SectionHeader>
      <Button>Explore Careers</Button>
    </section>
  )
}
